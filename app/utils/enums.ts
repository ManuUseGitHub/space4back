import { FIELDS } from "~~/server/utils/enums";
export enum TOAST_SEVERITY {
	INFO = "info",
	WARN = "warn",
	ERROR = "error",
	SUCCESS = "success",
	SECONDARY = "secondary",
	CONTRAST = "contrast",
}

export enum SECTION_STYLE {
	KEYS = "keys",
	LIST = "list",
}

export const FIELD_NAMES = FIELDS;
